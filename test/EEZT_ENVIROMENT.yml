- name: API test
  hosts: localhost
  vars:
    _default_enviroment: "{{ default_enviroment }}"
    _enviroment: "{{ enviroment }}"
  tasks:
    - name: Check ENV
      assert:
        that:
        - _default_enviroment == _enviroment
