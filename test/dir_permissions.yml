- name: Create dir and set permissions
  hosts: all
  tasks:
    - name: Create a directory if it does not exist
      file:
        path: "{{ dir_path }}"
        state: directory
        mode: "{{ permissions }}"
