- name: Hello Raul Sample
  hosts: all
  tasks:
    - name: Show date
      command: ls /tmp/
      register: reg
      
    - name: "Output test results"
      debug: 
        msg: "{{reg.stdout}}"

    - name: Store README file into /tmp
      fetch:
        src: /tmp/README.md
        dest: /tmp
