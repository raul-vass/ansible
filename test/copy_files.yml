- name: Hello Raul Sample
  hosts: all
  tasks:
    - name: Create a directory if it does not exist
      file:
        path: /tmp/
        state: directory
        mode: '0755'
      
    - name: Create new file
      copy:
      dest: "/tmp"
      content: HELLO WORD
      
    - name: "Output test results"
      debug: 
        msg: "{{reg.stdout}}"

    - name: Copy README file into remote host /tmp
      copy:
        src: ./README.md
        dest: /tmp
