- name: Hello Raul Sample
  hosts: all
  tasks:
    - name: Create readme file test
      command: echo "hola" > README.md
      register: reg
      
    - name: "Output test results"
      debug: 
        msg: "{{reg.stdout}}"

    - name: Copy README file into remote host /tmp
      copy:
        src: ./README.md
        dest: /tmp
